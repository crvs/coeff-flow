#pragma once

#include <scomplex/types.hpp>
#include <scomplex/quotient.hpp>
#include <scomplex/simplicial_complex.hpp>

/**
 * 
 **/

struct quotient::impl {
};

/* TODO
 * fill in details
quotient::quotient(const simplicial_complex&, bool(point_t));
quotient::quotient(const simplicial_complex&, bool(point_t), point_t);
quotient::quotient(const quotient&);
quotient::quotient& operator=(const quotient&);
quotient::~quotient();
quotient::std::shared_ptr<simplicial_complex> base_complex;
quotient::std::shared_ptr<simplicial_complex> quotient_complex;
quotient::chain_t quotient_chain(chain_t);
quotient::chain_v quotient_chain_v(chain_v);
quotient::chain_t unquotient_chain(chain_t);
quotient::chain_v unquotient_chain_v(chain_v);
*/
